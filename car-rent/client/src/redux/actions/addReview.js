// we did not integrated due to errors


// import axios from 'axios';

// // Action to add a new review
// export const addReview = (reviewData) => async (dispatch) => {
//   try {
//     // Make a POST request to the backend API endpoint to add the review
//     const response = await axios.post('/api/reviews', reviewData);

//     // Dispatch an action indicating that the review was successfully added
//     dispatch({ type: 'ADD_REVIEW_SUCCESS', payload: response.data });
//   } catch (error) {
//     // If there's an error, dispatch an action indicating the failure
//     dispatch({ type: 'ADD_REVIEW_FAILURE', payload: error.message });
//   }
// };
